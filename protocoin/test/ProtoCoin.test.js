const MetaCoin = artifacts.require("ProtoCoin");

contract("ProtoCoin", function (accounts) {
  it("should put 10000 ProtoCoin in the first account", function () {
    assert(true, "Error");
  });
});
